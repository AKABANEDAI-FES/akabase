-- ============================================================================
-- Seed Data for Development
-- ============================================================================
-- 注意: このファイルは開発環境専用です。本番環境では実行しないでください。

-- 現在のタイムスタンプ（ミリ秒）を使用
-- SQLiteでは変数が使えないため、各INSERTで直接値を指定します

-- ============================================================================
-- Users (認証用テストユーザー)
-- ============================================================================
INSERT INTO user (id, name, email, email_verified, image, created_at, updated_at)
VALUES
  ('user_admin', '管理者', 'admin@toyo.jp', 1, NULL, 1708560000000, 1708560000000),
  ('user_org_manager', '団体マネージャー', 'manager@toyo.jp', 1, NULL, 1708560000000, 1708560000000);

-- ============================================================================
-- Events (年度)
-- ============================================================================
INSERT INTO events (id, name, slug, status, created_at, updated_at)
VALUES
  ('event_2025', '東洋大学祭2025', '2025', 'active', 1708560000000, 1708560000000);

-- ============================================================================
-- Committee Roles (委員会役割)
-- ============================================================================
INSERT INTO committee_roles (id, event_id, user_id, role, created_at)
VALUES
  ('committee_2025_001', 'event_2025', 'user_admin', 'admin', 1708560000000);
